version: 2

builds:
  - dir: claude_code/wrapper
    main: .
    binary: strawpot_claude_code
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - formats: [binary]
    name_template: "strawpot_claude_code-{{ .Os }}-{{ .Arch }}"

release:
  github:
    owner: strawpot
    name: strawpot_claude_code_cli
